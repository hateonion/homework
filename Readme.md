## Initial Setup

Run `npm install` in project root.

## Start Dev Mode

Run `npm run dev`

## Running Tests

Run `npm test`

## Running e2e tests

Run `npm run e2e`

## Build for prod
Run `npm run build`
